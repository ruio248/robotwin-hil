# RoboTwin `handover_to_tray` 配对测评协议

**目的：** 让旧的 31% 结果与当前 SFT baseline、A/C 消融结果可以在相同场景上比较，避免把 seed 难度和场景筛选差异误当成模型差异。

## 1. 已知历史结果与最近一次不匹配运行

| 项目 | 旧记录 | 另一组 100 条有效 episode 测评 | 最近一次固定 seed 运行 |
|---|---|---|---|
| 结果 | SFT baseline `9999`: 31/100 = 31% | baseline（当时误标为 `9999`）: 50/100；A: 43/100；C: 47/100 | baseline: 0/100；A: 0/34（人工中止，非完整结果）；C 未运行 |
| seed | `31000–31099` | 从 `100000` 开始扫描，直到取得 100 个通过筛选的 seed；日志扫描到 `100108` | 固定 `31000–31099` |
| 专家检查 | 原始配置未完整保留 | `expert_check=true`，专家失败/初始化异常的 seed 会被跳过 | `expert_check=false` |
| 录像 | 原始配置不完整 | 视频开启，编码 10 fps | 视频关闭 |
| 频率/任务 | 原始配置不完整，需回查 | `handover_to_tray_v2_promptfix`，控制 30 Hz | `handover_to_tray_v2_promptfix`，控制 30 Hz、joint 动作 |
| baseline 实际权重 | 历史权重哈希未保留 | run config 指向 `iwr_sft_baseline_baseline_normstats_100_20260923/.../99`，并非已核实的 `9999` | 同样误指向 `...normstats_100_20260923/.../99` |

旧结果可从 `sft_policy_eval_100/eval_records.jsonl` 核对，覆盖 seed `31000–31099`，31 条成功记录。coverage 诊断的 `metrics.json` 复用了这些真实 rollout 成败标签；31% 不是 critic 预测的成功率。该诊断记录明确使用 100 条 rollout，而不是早期 34 条的部分汇总。已有诊断文档将策略标为 SFT baseline `9999`，但旧评测归档没有完整保留策略权重哈希及全部运行参数，因此在重新核对这些资产前，不能声称当前的 50% 与旧的 31% 是同一权重、同一协议下的重复测量。

**结论：** 31%、50% 和固定 seed 运行的 0% 不能直接相减。50% 来自另一组 seed、是专家筛选后的条件成功率，而且 baseline 权重路径实际指向 `..._100.../99`，并非已核实的 SFT `9999`。固定 seed 运行的 baseline 也误用了这个路径；因此 `0/100`、本轮中止前的 `0/45` 都不是 `9999` 的结果。旧代码还把 `expert_check` 与 instruction 来源耦合，两个因素必须分开核验，不能把差值归因于策略突然退化。

作为直接证据，同一个 A checkpoint 和相同的固定 manifest，旧固定 seed 尝试在 `expert_check=true` 下对前 24 个 seed 得到 9/24；最近 `expert_check=false` 的运行前 24 个 seed 为 0/24。旧运行随后在 seed 31024 因 scripted expert 检查失败而中止。此差异说明必须先锁定 instruction 生成，再做配对评测；由于录像设置也不同，不能把它归因于单一因素。

## 2. 主比较：复用旧的固定 100 个 seed

主比较集合固定为旧记录中的 `31000–31099`。把它写进一个不可变的 seed manifest，并让 baseline、A、C 每个 checkpoint 都运行同一份 manifest：

manifest 的 `test_seeds` 字段必须包含 `31000, 31001, ..., 31099` 这 100 个整数，不能在实际文件中用省略号代替。RoboTwin 当前评测入口支持通过 `--seed_manifest <文件> --seed_split test` 读取固定 seed 列表。

主比较规则：

1. 每个 checkpoint 对每个固定 seed 最多执行一次策略 rollout；禁止遇到难 seed 后自动递增 seed、补抽到 100 条，或按各 checkpoint 分别筛出 100 个“有效”场景。
2. 主结果使用 `--expert_check false --instruction_source scene_tags`。策略是否成功，只由策略 rollout 的任务成功判定决定；不能因为脚本专家失败就筛掉 seed。`instruction_source` 必须显式记录，且不能从 `expert_check` 推导。
3. `scene_tags` instruction 只由本次场景的 source/receiver tag 和 task instruction 模板构造，并使用 task seed 固定模板和描述抽样；同一个 seed 在 baseline、A、C 中必须得到完全相同的 instruction。每条 instruction 写入 eval log，便于事后核对。
4. 对策略开始前的环境初始化异常或不稳定场景，记录 seed、异常类型和阶段，不把它伪装成策略失败，也不静默换 seed。出现这类错误时中止整组配对比较；如果确认某些 seed 确实无法初始化，先形成一个**与 checkpoint 无关的共同可评测 seed 集**，再用同一集合重跑所有 checkpoint，并报告被排除 seed 和原因。
5. 一旦策略已经开始执行，超时、提前结束、动作异常或未达到任务终止条件均按该协议的策略失败处理；不可因失败而替换该 seed。
6. 若某个 seed 因评测基础设施问题（服务断连、仿真进程崩溃等）无法得到结果，应标成 infrastructure error，而不是成功或策略失败。修复后需对该 seed 的所有待比较 checkpoint 使用同样的重跑规则。

这样可以直接回答：“在旧 31% 的那批场景上，当前 baseline/A/C 各自表现如何？”如果历史权重文件和哈希仍可找到，还应把**原始权重**也加入同一固定 seed 比较；否则将新跑结果标为“对旧 seed 集的重测”，不称为原始 31% 的精确复现。

## 3. 所有 checkpoint 必须锁定的条件

在开始一组比较前，将下列信息写入每次运行的 `run_config`，并在 baseline、A、C 间保持一致；唯一预期变化是策略 checkpoint（以及该消融本身定义的数据来源或训练配置）：

- RoboTwin 代码版本/commit、任务名和任务配置；本轮采用 `task_name=handover_to_tray`、`task_config=handover_to_tray_v2_promptfix`。若旧版本配置无法确认，标记为无法精确复现旧结果。
- seed manifest 的文件内容和 SHA-256；每个 seed 的最终状态及跳过原因。
- `expert_check=false`、`instruction_source=scene_tags`，以及逐 seed 的最终 instruction 文本。
- SFT/消融 checkpoint 路径与权重哈希、policy serving 配置、`norm_stats` 资产名与哈希、任务提示词。
- 每个 checkpoint 必须加载与该 checkpoint 一起训练/发布的 `norm_stats`，并分别记录 asset ID 和 SHA-256；除非明确设计成统一 norm-stats 消融，不得为了“看起来一致”把所有模型强行换成同一份统计量。本轮真 SFT `9999` 使用 checkpoint 自带的 `ruio248/robotwin_handover_to_tray_v2_promptfix`；A/C 使用训练时附带的 `baseline_baseline_normstats_allframes_v1`。因此 norm-stats 差异是本轮对照的一部分，不能在报告中隐藏。
- `action_type=joint`、控制频率 30 Hz、动作 chunk 的推理和执行逻辑、最大 episode 步数与成功判定函数。
- 相同的相机/场景配置、仿真参数、reset 过程、服务端配置以及单实例运行方式。
- 视频可以按 10 fps 编码，但必须明确它只是录像帧率，不是控制频率；视频设置不得改变仿真控制节奏。

比较时保留逐 seed 的成功/失败记录，除了总体成功率，还报告三种策略在同一 seed 上的配对结果、差值及配对置信区间。100 个 seed 的结果应标为有限样本诊断，不应只给一个百分比而不附分母和 seed 集。

## 4. `expert_check` 的次级报告口径

可以额外计算一个“脚本专家可解场景上的条件成功率”，但它是**次级指标**，不能替代主比较：

- 对每个 seed 记录专家预检是成功、失败、环境不稳定还是执行异常。即使启用 `expert_check=true`，若目的是比较专家筛选效果，仍应显式设置 `instruction_source=scene_tags`；否则 expert rollout 还会改变任务 instruction。
- 先生成一次公共的专家通过 seed mask，再让所有 checkpoint 使用完全相同的 mask；不得为每个 checkpoint 单独筛 seed。
- 同时报告候选 seed 数、专家通过数、跳过数/原因、策略成功数和最终分母。
- 结果命名为 `expert-filtered / expert-solvable conditional success rate`，不要简写成该策略在完整测试分布上的成功率。使用专家生成的 instruction 应另作为独立实验条件报告，不能和 scene-tag instruction 的结果混在一起。

`expert_check` 与 instruction 的来源在旧版脚本中耦合：启用时用 `play_once()` 的 episode info 生成 instruction，关闭时改用 task-env 的 scene tags。新版脚本增加独立的 `--instruction_source`，并在给定 seed 时固定 instruction 模板/描述的随机选择；主比较应使用 `scene_tags`。固定 manifest 模式遇到策略启动前的初始化错误会直接失败退出，而不是自动构造共同 mask。正式配对跑之前要先确认所有 seed 均可初始化；若遇到异常，停止整组、诊断后对所有 checkpoint 使用同一份共同有效 manifest，绝不静默补抽。

## 5. 本轮建议执行顺序

1. 从旧 `eval_records.jsonl` 生成并校验固定 manifest：100 个唯一 seed，严格为 `31000–31099`；记录文件哈希。
2. 回查旧 31% 的策略 checkpoint、task config、expert filter、控制频率和成功判定。无法找回的字段应在报告中写成“未知”，不能猜测。
3. 使用 `expert_check=false`、`instruction_source=scene_tags` 对固定 manifest 逐 seed 做策略前环境初始化；不得以 scripted-expert 成功与否筛主测试集。固定 seed 初始化异常时整组中止并记录，不补抽；若确认需要排除异常 seed，则先构造共同 manifest，再从头重跑全部 checkpoint。
4. 在同一环境与服务条件下，依次评测当前 SFT baseline `9999`、A、C（以及可用时的原始 baseline 权重），每个 seed 只评测一次；保存逐 seed instruction、结果和失败视频。三组统一开启录像，MP4 编码 10 fps，控制仍为 30 Hz。
5. 汇总时同时给出每组 `successes / common_valid_seeds`、排除 seed 及原因、运行配置与权重/norm-stats 哈希；对同一 seed 的策略差异做配对分析。
6. 如另行运行 `expert_check=true` 的结果，将其放在次级表中，并明确注明它只衡量专家预检通过场景上的条件成功率。

## 6. 已知限制

- 旧的 31% 记录保存了 100 条 seed `31000–31099` 的 rollout 结果，但没有完整保存原评测配置和策略权重哈希；因此在资产核验前，固定 seed 重测只能解决**场景集合对齐**，不能自动证明其余历史条件也完全一致。
- 新的 50% 使用另一批 seed，且启用了 `expert_check`。它适合作为专家筛选条件下的观测值，不应直接与旧 31% 作因果比较。
- 最近一次固定 seed 运行的 baseline `0/100`、A `0/34` 是在 `expert_check=false` 但未固定 instruction 来源、且关闭录像的条件下得到。A 在 34 条时由操作者中止，不能报告成完整 100 条结果；C 未运行。这批结果不作为策略基准发布。
- 2026-09-24 的 `scene_tags` 运行已作废：虽然组名写作 `baseline_sft9999`，实际加载的是 `iwr_sft_baseline_baseline_normstats_100_20260923/checkpoint/99`，不是 SFT `9999`；跑到 seed `31044` 时累计 `0/45`，随后因权重路径错误停止。该数值不能作为 `9999` 基线结果。
- 若主比较中必须排除初始化失败 seed，所有 checkpoint 必须使用共同有效子集；报告的成功率分母是共同有效 seed 数，而不是原始候选数或某个模型单独凑够的 episode 数。
