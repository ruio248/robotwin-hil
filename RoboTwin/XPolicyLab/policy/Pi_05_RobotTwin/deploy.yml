# Basic experiment configuration
policy_name: Pi_05_RobotTwin
protocol: ws
host: localhost
bench_name: null
task_name: null
env_cfg_type: null
seed: null
eval_batch: true

checkpoint_num: 1000
result_dir: "./results/robotwin_handover_to_tray/"
obs_transform_pipeline: "xspark-v1.0"

# Model / action configuration
action_type: joint
policy_uv_env_path: openpi
# Must match the config used by train.sh (default OPENPI_TRAIN_CONFIG_NAME).
# The private RobotTwin training script uses the standard 14D Aloha Pi0.5
# config; the RobotTwin dataset/repo id is supplied separately below.
train_config_name: pi05_base_aloha_full_sim_arx-x5_seed_0
repo_id: ruio248/robotwin_handover_to_tray_v1

port: 6000
